## Python Control Interface

To launch the control interface on your machine:

1. Open a terminal in the `logturtle_I2C_Interface` folder
2. Install the package:
   `pip install -e .` (with the full stop)
3. Launch the interface via a Python script:
   from logturtle.core import main
   main()
